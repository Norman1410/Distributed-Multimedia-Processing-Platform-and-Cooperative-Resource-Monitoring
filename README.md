# Proyecto Multimedia Distribuido

Plataforma distribuida para procesamiento multimedia usando Python, Docker y Redis.

Arquitectura:
Cliente → Coordinador → Cola (Redis) → Workers → Resultados → Dashboard